<!DOCTYPE html>
<html lang="<%= I18n.locale %>">
<head>
  <title><%= content_for(:title) || t('site.title') %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="<%= content_for(:description) || t('site.description') %>">
  <meta name="keywords" content="security systems, IoT, sustainable energy, smart home, automation, fire alarms, renewable technology, technology integration">
  <meta name="author" content="Nicolas Mahecha">
  <meta name="robots" content="index, follow">
  
  <!-- Open Graph / Facebook -->
  <meta property="og:type" content="website">
  <meta property="og:url" content="<%= request.original_url %>">
  <meta property="og:title" content="<%= content_for(:title) || t('site.title') %>">
  <meta property="og:description" content="<%= content_for(:description) || t('site.description') %>">
  <meta property="og:image" content="<%= asset_url('profile.jpg') %>">

  <!-- Twitter -->
  <meta property="twitter:card" content="summary_large_image">
  <meta property="twitter:url" content="<%= request.original_url %>">
  <meta property="twitter:title" content="<%= content_for(:title) || t('site.title') %>">
  <meta property="twitter:description" content="<%= content_for(:description) || t('site.description') %>">
  <meta property="twitter:image" content="<%= asset_url('profile.jpg') %>">

  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= yield :head %>

  <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  
  <!-- Fonts and Icons -->
  <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@500;700&display=swap" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/@mdi/font@7.2.96/css/materialdesignicons.min.css" rel="stylesheet">
  
  <%= javascript_importmap_tags %>
  <link rel="icon" href="<%= asset_path 'favicon.svg' %>" type="image/svg+xml">
  
  <!-- Google tag (gtag.js) -->
  <script async src="https://www.googletagmanager.com/gtag/js?id=G-TCDDR17DBW"></script>
  <script>
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());

    gtag('config', 'G-TCDDR17DBW');
  </script>
</head>

<body class="bg-gray-900 text-white font-sans">
  <!-- Header -->
  <header class="nav-container flex items-center justify-between flex-wrap">
    <a href="/" class="nav-brand">Nicolas Mahecha</a>
    <!-- Hamburger for mobile -->
    <button id="mobile-menu-button" class="sm:hidden ml-auto text-white focus:outline-none" aria-label="Open menu" onclick="document.getElementById('mobile-menu').classList.toggle('hidden')">
      <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path></svg>
    </button>
    <!-- Desktop Nav -->
    <nav class="nav-menu hidden sm:flex space-x-8">
      <a href="<%= about_path %>" class="nav-link"><%= t('navigation.about') %></a>
      <a href="<%= projects_path %>" class="nav-link"><%= t('navigation.projects') %></a>
      <a href="<%= contact_path %>" class="nav-link"><%= t('navigation.contact') %></a>
    </nav>
    <!-- Language Switcher always visible on right -->
    <div class="flex items-center space-x-2 ml-4">
      <%= render "shared/language_switcher" %>
    </div>
    <!-- Mobile Nav -->
    <div id="mobile-menu" class="w-full sm:hidden hidden mt-4">
      <nav class="flex flex-col space-y-2 bg-gray-900 rounded-lg p-4">
        <a href="<%= about_path %>" class="nav-link"><%= t('navigation.about') %></a>
        <a href="<%= projects_path %>" class="nav-link"><%= t('navigation.projects') %></a>
        <a href="<%= contact_path %>" class="nav-link"><%= t('navigation.contact') %></a>
        <div class="mt-2">
          <%= render "shared/language_switcher" %>
        </div>
      </nav>
    </div>
  </header>

  <main>
    <div id="flash" class="fixed top-4 right-4 z-50 w-96"></div>
    <%= yield %>
  </main>

  <!-- Footer -->
  <footer class="bg-gray-900 border-t border-gray-800 py-10">
    <div class="container mx-auto px-8 text-center">
      <h4 class="text-xl font-bold">Nicolas Mahecha</h4>
      <p class="text-gray-400">Security Systems | Smart Device Integration | Software Development</p>
      <div class="flex justify-center space-x-6 mt-4">
        <a href="https://www.linkedin.com/in/nm85/" target="_blank" class="text-gray-400 hover:text-teal-400 transition-colors">LinkedIn</a>
        <a href="https://github.com/nicolasmd1985" target="_blank" class="text-gray-400 hover:text-teal-400 transition-colors">GitHub</a>
      </div>
      <p class="text-gray-500 mt-6">&copy; <%= Time.current.year %> Nicolas Mahecha. <%= t('footer.rights') %>.</p>
    </div>
  </footer>
</body>
</html>
